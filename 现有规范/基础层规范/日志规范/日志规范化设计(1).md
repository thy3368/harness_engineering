# 日期
## 版本
## 修改人
## 操作说明
# 20250425
1.0
李宗
初稿
密级

秘密
## 版本记录


## 引言

## 业务背景

## 功能需求

## 按照公司架构办日志规范提取需求点。
1. 提供logproxy接口满足不同类型的日志记录，支持不同类型错误记录在不同的日志
2. 错误号重新定义
3. remoter、front、bizproc接口调用改造计划，需匹配正确的接口和错误号
4. remoter不得出现运维错误，需要将remoter回调给上层，其他模块可以出现程序错误、运维错误
5. 支持不同类型日志路径可配置
6. 按照最新的格式产生日志
7. 支持日志归档和不同类型日志清理天数不同的配置？？？
## 非功能需求

1. 性能准出标准
待补充
2. 安全性需求
敏感信息的屏蔽要做到不可逆性
## 详细设计

## 日志规范化

日志分为两部分：

日志头部信息：
日志信息
分为两部分info ext
## 日志统一格式：
平台调试日志(.debuglog)

开发调试
## 日志格式
ext：调试号 调试信息
log_version timestamp log_level processId-Ttid systemName-moduleName-MfunName-
LlineNum formatType type info ext
log_version timestamp log_level processID-Ttid systemName-moduleName-MfunName-
LlineNum formatType TP0 version 1 ext
V2.0 2025-09-12 21:17:03.138490 DEBUG 8960-T7589 MOnMessage-Lfront/utils.cpp:116
FS TP0 V3.0 1 131 [调试 (value=1)]

链路日志(.tracelog)

基于可观测性做的后台埋点日志，主要作用是性能分析、故障排查和依赖分析
## 日志格式
由于新格式在refs外已经包含了部分信息，所以，将timelog中的线程id、错误码删除。
如果是业务返回错误，错误信息暂时为空，这里模拟的是平台错误。链路信息行号信息为空。
log_version timestamp log_level processID-Ttid systemName-moduleName-MfunName-
LlineNum formatType TP1 trace_id spanid deport_id module_type responseStatus
version serverName appType charset error_no error_info error_infox app_protocol
req_body rep_body refs
V2.0 2025-09-12 21:17:03.138490 INFO 48860-T2789 - FS TP1 0xf12938580607 0.1 0 1
V3.0 11 ldp_bizproc 9 1 0 1542 [转发失败] - ldp reqbase64nidjfllg94885jdfn -
[.TName bizproc_worker-1 .Type Req .Eime 1719370 .MergeRsp 1 .CommPattern 0x21
PluginRecv 1757986726860887070 BizRspStart 16530 BizRspEnd 30 ForwardRsp 130
PluginSend 550]

业务日志(.bizlog)

请求执行报错，联机异常不需要运维干预；批处理打点
## 日志格式
平台事件日志(.eventlog)

## 记录重要事件，用于问题排查
## 日志格式
log_version timestamp log_level processID-Ttid systemName-moduleName-MfunName-
LlineNum formatType TP2 trace_id spanid deport_id responseStatus version
server_name operator_id cust_id charset error_no error_info error_infox
V2.0 2025-09-12 21:17:03.138490 ERROR 4657-T2950 MF973001-
Ls_uftmdb_uses_lsu_sespolling_flow.cpp,455 FS TP2 0xf12938580607 0.1 0 0 V2.3.1
ldp_bizproc - - 1 372 [[372][轮询处理错
误:function(F973001),error_no(372),error_info([372][交易参数表:usps_exch_arg(唯
一:idx_usps_exch_arg)记录不存在][p_exchange_type=3]),error_pathinfo(F973001-
log_version timestamp log_level processID-Ttid systemName-moduleName-MfunName-
LlineNum formatType TP3 version operator_id charset ex
V2.0 2025-09-12 21:17:03.138490 EVENT 6779-T289 MInit-
Lbizproc/bizproc_plugin_impl.cpp:116 FS TP3 V3.0 - 1 [实例 ldp_billy_33-xstA#0 初
始化开始]

ext：事件号 事件信息
平台运维日志(.sysopslog)

操作系统、磁盘、内存、网络等基础设施和硬件错误； 运维参数设置错误
## 日志格式
平台警告日志(.warninglog)

证书快到期、队列排队、超时重试
## 日志格式
log_version timestamp log_level processID-Ttid MfunName-LlineNum formatType TP6
trace_id spanid deport_id responseStatus version server_name operator_id cust_id
charset error_no error_info error_infox
参见程序运行日志。运维日志 traceid、span_id、depot_id可以为空。
log_version timestamp log_level processID-Ttid systemName-moduleName-MfunName-
LlineNum formatType TP7 version 1 ext error_no error_infox
V2.0 2025-09-12 21:17:03.138490 WARN 68709-T7589 ldp_mproxy-auth-MauthFun-
L../utilities/ldp_auth/ldp_auth_impl.cpp:353 FS TP7 V3.0 1 4011 [找不到授权文件路径
## ./ldplicense/ 警告信息 没有那个文件或目录]

ext：事件号 事件信息，后面的error_no error_info为空（这里涉及到第三方解析问题，所以需要找长
老再确认一下）
程序运行日志(.runtimelog)

包括请求执行中的程序缺陷错误和非服务技术组件（比如SDK）日志。前者用于开发人员定位和解决错
误，后者主要用于排查异常。
## 日志格式
error_infox中暂时不携带行号与错误码：8740 MSend-Lbackend_mgr.cpp:1710
原始示例：
附件：业务日志
10.20.197.64_20250920-hsldp-ldp_sys_mdb_xn-usesbid62#0.bizlog
## 方案设计

数据来源分析

log_version timestamp log_level processID-Ttid systemName-moduleName-MfunName-
LlineNum formatType TP9 trace_id spanid deport_id responseStatus version
server_name operator_id cust_id charset error_no error_info error_infox
V2.0 2025-09-12 21:17:03.138490 ERROR 37859-T24 MForwardRequeset-
Lfront_plugin_impl.cpp:1710 FS TP9 0xf12938580607 0.1 0 0 V3.0 ldp_front - - 1
1752 [转发请求失败（tyep=36，Function=501，8740)] [未知BackendId65535，消息功能号501类
型36连接号1207959552]

log_version
固定值v2.0
time_stamp
上层调用日志接口的时间戳
log_level
日志宏传入。 链路日志固定INFO
server_info
支持外部自定义传入（后续可能需要支持）和 logproxy直接获取写入
（进程号与线程号）
location
系统与功能名称由上层设置给CLogWriter，函数名、行号其使用宏，
结合拼接位置。系统使用插件名、功能名称使用运行状态(待讨论)
format
logproxy内部确定，目前使用空格 方式，固定值FS
type
外部调用不同的日志宏传入不同的日志类型。格式为TP+编号
version
## 系统版本（插件版本？？）。
charset
logproxy内部配置，暂时固定值为1
traceid、span_id、
deport_id、
response_status
使用者传入logproxy, 存储于reader/writer/head中
server_name
插件名，上层传入
app_type
logproxy 内部使用数值9（ldp）
charset
logproxy 暂时内部写成固定值1（utf-8）
response_time
## 应答与请求的差值。（需要单独设计）暂停为-
拓展信息(ext)
error_no、error_info：上层的（服务模块）信息
error_infox：底层模块（remoter rcm）返回/回调的错误信息
模块功能实现

1. remoter提供回调接口，或者提供可以直接获取错误信息的接口，其他上层模块重载remoter的
OnErrorEvent函数
2. logproxy实现新文件格式创建的功能，内部实现新格式的写入 。
3. 业务日志去掉/修改其他类型日志，只保存INFO、ERROR
4. 宏支持
5. 上层模块调用新日志宏
整体方案

方案一

需指定日志级别和日志类型，可以根据底层错误，决定自己是沿用以前的代码风格上层，根据错误号，
调用不同的日志宏。
配置项可用方案二的设计，不过数据结构由每个插件维护。
兼容性考虑：需要产生新版本日志的地方调V2日志宏即可。

方案二

无需指定日志级别和日志类型
将配置信息（数据结构）导入CLogWriter，其根据不同的错误号描述，决定写入不同的文件
## 配置文件设计：
数据结构(AIGC协助)：
时间复杂度O(logn)
方案三（方案二的优化版）

不进行单独的文件配置，使用message_level中的值。
错误号字典新增字段表明日志类型，值参照：后续设计中的：LoggerType
默认值为0。使用原始的逻辑。
//配置于根节点
//支持模糊匹配
//支持配置校验
//如果错误号重叠，启动时告警，运行时优先作用于靠前的配置项
//闭区间
{
"DebugNumRange":"1?-2?",
"InfoNumRange":"40-100",
"WarnNumRange":"100-1000",
“RunTimeInfoNumRange”:"2000-4?44",
“RunTimeErrorNumRange”:"5000-5???",
“RunTimeFATALNumRange”:"6000-6???",
"SysOpsErrorNumRange":"7000-7???",
"SysOpsErrorNumRange":"8000-8???"
}

## 模块设计

front

1.重载remoter回调函数实现方法
错误描述示例：
回调行为需特殊处理。
## 格式化错误信息已经由（底层）remoter完成
2. 打点实现
V2版本的日志生成调用EndTimeStampV2接口，补充错误号与错误信息(如果外部可以传入)。
remoter

remoter中包含以下几种类型的错误。
实时监测、启动停止期、运行期（消息触发）。
实时监测

实时监测采用回调的方式。
实时监测类，remoter不用调用日志接口，只需要上送即可。
启动停止

方案一：直接返回错误码与错误信息，函数调用层级比较深
方案二：try catch方案，外部都要捕获，外部改动点比较大
方案三：线程局部存储（使用方案三）
错误信息上下文
int32_t OnErrorEvent(const ErrorInfoxContext& errorInfoxContext)
{
//根据错误号结合当前状态决定要落入的日志类型。
//告警日志
m_lpILogProxy->WriteV2(nErrorNo，WARN, const wchar_t *lpErrorInfo);
//
可以直接调用
}
class ILdpRemoterCallBack
{
//错误信息回调
virtual int32_t OnErrorEvent(const ErrorInfoxContext& errorInfoxContext)
noexcept = 0;
}

提供外部获取接口
## 变量声明
逻辑实现：
直接调用LOG_V2宏，通过logwriter、mgsformat获取位置、错误信息，并且设置到errorContext中。
由外部获取
运行期

同上。
log_writer

日志错误信息存储与加载
为了支持运维日志、程序运行日志中包含链路信息，所以需要支持reader以及writer传入
struct ErrorInfoxContext
{
wchar* szLineInfo = nullptr;//为了后续拼接也是用wchar*
int32_t errorNum = 0;
wchar* szErrorInfo = nullptr;
}
class ILdpRemoter
{   //获取当前线程最后一次错误信息
virtual ErrorContext* GetLastErrorContext() noexcept = 0;
}
class ILdpRemoter
{
private:
//线程局部存储
thread_local ErrorContext errorContext;
}
class CMsgDef
{
public:
uint32_t GetLogType(int32_t nErrorNo);
int32_t LoadLevlAndLogType();
private:
cosnt uint32_t         m_uDefaultLogType = 0;
std::map<int32_t, int> m_MapErrType;//<<错误号，日志类型>>
}
通过错误号日志类型，决定调用后续的宏
//适用于debug warn event
#define LOG_V2（nErrorNo,...)
LDPLOGGER_UNLIKELY_V2(m_lpLogWriter,nErrorNo,LEVEL_INFO,##_VA_ARGS_)
//下面3个适用于runtime sysops
#define LOG_Reader_V2（nErrorNo,szErrorInfoEx,lpLdpReader...)

内部函数实现(伪代码)：
#define LOG_Writer_V2（nErrorNo,szErrorInfoEx,lpLdpWriter...)
//由于remoter可能没有解包器
#define LOG_Head_V2（nErrorNo,szErrorInfoEx,lpLdpHead...)
//调试、警告、事件不需要链路信息，那么不需要解包器
//.debuglog
#define LOG_DEBUG_V2（nErrorNo,...)
LDPLOGGER_UNLIKELY_V2(m_lpLogWriter,nErrorNo,LEVEL_DEBUG,##_VA_ARGS_)
//（.eventlog）
#define LOG_EVENT_V2（nErrorNo,...)
LDPLOGGER_UNLIKELY_V2(m_lpLogWriter,nErrorNo,LEVEL_EVENT,##_VA_ARGS_)
//（.warninglog）
#define LOG_WARN_V2（nErrorNo,...)
LDPLOGGER_UNLIKELY_V2(m_lpLogWriter,nErrorNo,LEVEL_WARN,##_VA_ARGS_)
由于运行日志和运维日志需要链路信息，那么需要传入解包器
//（.runtimelog）支持reader传入
#define LOG_INFO_RUN_Reader_V2（nErrorNo,szErrorInfoEx,lpLdpReader...)
LDPLOGGER_UNLIKELY(m_lpLogWriter,nErrorNo,szErrorInfoEx,lpLdpReader,LEVEL_INFO,#
#_VA_ARGS_)
#define LOG_ERROR_RUN_Reader_V2（nErrorNo,szErrorInfoEx,lpLdpReader...)
LDPLOGGER_UNLIKELY(m_lpLogWriter,nErrorNo,szErrorInfoEx,lpLdpReader,LEVEL_WARN,#
#_VA_ARGS_)
#define LOG_FATAL_RUN_Reader_V2（nErrorNo,szErrorInfoEx,lpLdpReader...)
LDPLOGGER_UNLIKELY(m_lpLogWriter,nErrorNo,szErrorInfoEx,lpLdpReader,LEVEL_FATAL,
##_VA_ARGS_)
//同理也要支持writer Head指针传入(实现省略)
LOG_INFO_RUN_??_V2（运行日志）（.runtimelog）
LOG_ERROR_RUN_??_V2（运行日志）（.runtimelog）
LOG_FATAL_RUN_??_V2（运行日志）（.runtimelog）
//参见运行日志（实现省略）
LOG_ERROR_SYS_??_V2（运维警告日志）(.sysopslog)
LOG_FATAL_SYS__??V2（运维严重错误日志）(.sysopslog)
class CLogWriter{
public:

cosnt wchar* GetLocationInfo()
{
return strLocation.c_str();
}
//这里仅描述Reader传入的方法，需补充writer和head类型的函数实现
void CLogWrite::LogV2(ILdpMsgReader* lpLdpMsgReader, LoggerType logType,
int32_t nErrorNo, int32_t nLevel, wchar_t *szErrorInfox, const char *szLine,
cosnt char* szFunction,...)
{
//拼接位置信息
FormatLocationInfo()
//格式化错误信息拼接ErrorInfo和szErrorInfox
FormatExtInfo()
//调用logproxy接口
m_lpILogProxy->WriteV2(lpLdpMsgReader, logType, nErrorNo, nLevel,
m_lpMesgFormat-       >GetMessage(),szLocation)

业务日志已经支持reader，宏无需改动,见biz_data.h头文件。暂时不支持业务日志传入底层错误信息。
logproxy

接口
}

//szErrorInfox预留 业务暂时不用传入
void CLogWrite::BizLogV2(ILdpMsgReader* lpLdpMsgReader, LoggerType logType,
int32_t nErrorNo, int32_t nLevel, wchar_t *szErrorInfox, const char *szLine,
cosnt char* szFunction,...)
{
//拼接位置信息
FormatLocationInfo()
//格式化错误信息拼接ErrorInfo和szErrorInfox
FormatExtInfo()
//调用logproxy接口
m_lpILogProxy->WriteV2(lpLdpMsgReader, logType, nErrorNo, nLevel,
m_lpMesgFormat-       >GetMessage(),szLocation)
}
//提供设置系统和功能的接口用于拼接位置信息
void SetSystemName(cont char* szSystemName)
{

}
void SetModulemName(cont char* szModulemName)
{

}
privte:
thread_local std::wstring strLocation;
}
class ILogProxy{
public:
//除了业务日志、链路日志，都用这个接口
//较原始接口拆分原来错误信息为位置信息+错误描述(包含当前错误和底层错误)，消息操作对象
virtual int32_t WriteV2(ILdpMsgReader *lpMsgReader,LoggerType logType,
int32_t nErrorNo, LogLevel eLevel, const const wchar_t *lpExt, char* lpLocation,
uint32 iFlag = OUTPUT_FILE) = 0;

virtual int32_t WriteV2(ILdpMsgWriter *lpMsgWriter,LoggerType logType,
int32_t nErrorNo, LogLevel eLevel, const const wchar_t *lpExt, char* lpLocation,
uint32 iFlag = OUTPUT_FILE) = 0;
}
class ILogProxyEx{
public:
//业务日志接口
//较原始接口拆分原来错误信息为位置信息+错误描述(包含当前错误和底层错误)
virtual int32_t WriteBizV2(ILdpMsgReader *lpMsgReader,int32_t nError,
LogLevel eLevel, const wchar_t *lpExt, const char* lpLocation)

//.tracelog 支持writer与reader传入，补充错误信息
virtual int32_t EndTimeStampV2(ILdpMsgWriter* lpLdpMsgWriter, ITimeStamp*
lpTimeStamp,int nErrorNo, const wchar_t* lpErrorInfo)  = 0;

新增日志类型
创建文件

日志写入

线程模型

virtual int32_t EndTimeStampV2(ILdpMsgReader* lpLdpMsgReader, ITimeStamp*
lpTimeStamp,int nErrorNo, const wchar_t* lpErrorInfo)  = 0;
}
enum LoggerType :uint32t
{
DEBUGLOG=0,//.debuglog
TRACELOG=1,//.tracelog
BIZLOG=2,//.bizlog
EVENTLOG=3,//.eventlog
SYSOPSLOG=6, //.sysopslog
WARNINGLOG=7,//.warninglog
RUNTIMELOG=9,//.runtimelog
}
enum LogLevel ：uint32_t
{
DEBUG=0x01,
INFO=0x02,
WARN=0x04,
ERROR=0x08,
FATAL=0x10,
}
## //声明新的CLogWriter对象
class ClogProxyImpl
{
privte:
CLogWrite *m_lpDebugLogWrite{nullptr};
CLogwrite*m_lpTraceLogWrite{nullptr};
CLogwrite*m_lpTimeLogwrite[nullptr];
CLogWrite*m_lpBizLogWrite[nullptr];
CLogWrite *m_lpEventLogWrite(nullptr};
CLogwrite *m_lpSysOpsLogWrite{nullptr};
CLogWrite *m_lpWarningLogWrite(nullptr};
CLogwrite *m_lpRuntimeLogWrite{nullptr};
}
int32_t CLogProxyImpl::Init(const char *lpWorkDir, const char* *lpFileName)
{
//todo创建新文件 参考log创建逻辑
//.tracelog无需考虑合并。其他日志需要考虑合并
}

## 兼容性设计

按照分期计划，logproxy需兼容原始日志输出和新版日志输出。
第三方兼容？？
补充
## 模块依赖说明

如果需要使用新版的日志格式，使用者需要依赖logproxy和hsldp
## 测试点设计

//所有信息log_version timestamp log_level processID-Ttid systemName-moduleName-
MfunName-LlineNum formatType TP1 trace_id spanid deport_id module_type
responseStatus version serverName appType charset error_no error_info error_infox
app_protocol req_body rep_body refs
//所以日志最终写入公用一个函数
int32_t WriteLogInternalV3(
struct timeval *tv,//时间戳信息，支持异步同步
LogLevel eLevel, //日志等级
int32_t tid,//线程号
const char* szlocation, //位置信息
LoggerType logType, //日志类型
const uint8_t *lpTraceId,//链路ID
uint32_t uDeportId,//
uint64_t *lpSpanId,//
uint32_t uSpandIdLevel, //错误号
const wchar_t* lpErrorInfo,//错误信息
size_t nSrcLen,//错误信息字符串长度
TimeStamp *lpTimeStamp,//timelog特有的
)
{
//日志头的拼接逻辑一致，建议封装成一个函数
//根据不同的日志类型拼接不同的字符串
//需要考虑拓展头和可选头的traceid
}

## 发布版本
存在依赖的模块
被依赖模块
## 被依赖模块发布版本
备注


任务序号
开发任务简述
开发工时评估（人日）
logproxy
支持新版日志
7
hsldp
提供日志宏
3
remoter
## 确定错误号范围，回调错误信息
3
front
对底层错误判断，调用新日志宏
2
1. 兼容性测试，保证以前的日志正常输出
2. 查看新版本格式是否与描述一致
3. 参照错误列表描述 查看落入的文件是否正确
4. 链路日志需能在日志中心上正常采集绘制
## 非功能测试点设计

## 验证新版本链路日志对高性能下tps的影响
## 约束说明

## 只有调用了新版本日志宏，并且配置了错误号范围才能输出到V2日志中。
## 模块依赖说明

工作量评估


遗留问题清单


## 评审记录

评审时间 20250922
评审人：董攀峰、胡细笔
评审纪要：
1. 有些格式没有统一，多余时延字段和业务信息还存在。（需要让同长老再确认下）
2. 确认链路原则，为什么非链路日志里面，也包含链路信息。链路信息与其他日志信息有重叠的地
方，导致一个错误信息再很多地方被重复记录。（需要让同长老再确认下）
3. []的方式可能遇到转义的问题，内部格式方案，待格式确定后确认。
4. 进程名改成进程号

序号
AP项
责任人
计划完成时间


5. 暂停location格式：插件名-函数名-行号 （确认方案中接口跟方案的区别）
6. 后续需要设计补充的
全部格式确定，每个参数怎么传，哪个模块来做，接口设计。
AP列表：

